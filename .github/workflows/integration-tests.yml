name: Run Agentex Integration Tests

on:
  pull_request:
    paths:
      - 'agentex/**'
  push:
    branches:
      - main
    paths:
      - 'agentex/**'
  workflow_dispatch:
    inputs:
      commit-sha:
        description: "Commit SHA to test against"
        required: true
        type: string
