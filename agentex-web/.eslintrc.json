{
  "extends": ["next/core-web-vitals", "next/typescript", "plugin:storybook/recommended"],
  "rules": {
    "no-restricted-imports": [
      "error",
      {
        "paths": [
          {
            "name": "react",
            "importNames": ["default"],
            "message": "Import React using named imports instead: import { useState, useEffect } from 'react'"
          }
        ],
        "patterns": [
          {
            "group": ["react"],
            "importNamePattern": "^\\* as React$",
            "message": "Use named imports instead of namespace imports: import { useState, useEffect } from 'react'"
          }
        ]
      }
    ]
  }
}
